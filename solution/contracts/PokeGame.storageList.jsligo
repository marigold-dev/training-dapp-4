#import "PokeGame.jsligo" "Contract"

const default_storage: Contract.PokeGame.storage = {
    dynamic_entrypoints: Big_map.empty as big_map<nat, bytes>,
    storage: {
        pokeTraces: Map.empty as map<address, Contract.PokeGame.pokeMessage>,
        feedback: "kiss",
        ticketOwnership: Map.empty as
            map<
                address,
                ticket<string>
            > //ticket of claims

    },
};
